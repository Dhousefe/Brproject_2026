package ext.mods.gameserver.handler.voicedcommandhandlers;

/** Relocated to modules/mods/mod-email (same package). Marker only; excluded from core compile. */
final class Email_relocated
{
	private Email_relocated()
	{
	}
}
