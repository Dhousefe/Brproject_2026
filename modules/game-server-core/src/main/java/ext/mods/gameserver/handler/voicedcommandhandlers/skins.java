package ext.mods.gameserver.handler.voicedcommandhandlers;

/** Relocated to modules/mods/mod-dressme (same package). Marker only; excluded from core compile. */
final class skins_relocated
{
	private skins_relocated()
	{
	}
}
