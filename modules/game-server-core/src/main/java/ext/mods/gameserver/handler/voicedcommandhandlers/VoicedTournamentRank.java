package ext.mods.gameserver.handler.voicedcommandhandlers;

/** Relocated to modules/mods/mod-tour (same package). Marker only; excluded from core compile. */
final class VoicedTournamentRank_relocated
{
	private VoicedTournamentRank_relocated()
	{
	}
}
