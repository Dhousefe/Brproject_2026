package ext.mods.gameserver.handler.voicedcommandhandlers;

/** Relocated to modules/mods/mod-battle-boss (same package). Marker only; excluded from core compile. */
final class VoicedBossBattle_relocated
{
	private VoicedBossBattle_relocated()
	{
	}
}
