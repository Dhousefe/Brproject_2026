package ext.mods.gameserver.handler.bypasshandlers;

/** Relocated to modules/mods/mod-farm-event (same package). Marker only; excluded from core compile. */
final class FarmZoneTeleportBypass_relocated
{
	private FarmZoneTeleportBypass_relocated()
	{
	}
}
